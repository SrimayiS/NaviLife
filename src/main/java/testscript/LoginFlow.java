package testscript;

import org.testng.annotations.Test;

import generic.Autoconstant;
import generic.Preconditions;
import pom.Login_Page;

public class LoginFlow extends Preconditions{
	
	
	@Test
	public void invalidEmailFlow() {
		
		/*
		 * Login_Page Lp = new Login_Page(driver); Lp.setEmail(Autoconstant.email);
		 * Lp.setPassword("jhj"); Lp.clickLogin(); Lp.
		 * verifyEnterEmailErrorMessage("The email address or mobile number you entered isn't connected to an account. Find your account and log in."
		 * );
		 * 
		 */}

}
